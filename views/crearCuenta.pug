<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login

        .contenedor-formulario
            h1 Crea tu cuenta en UpTask

                if locals.mensajes
                    .alertas
                        each categoria in Object.keys(locals.mensajes)
                            each error in mensajes[categoria]
                                .alerta(class=`${categoria}`) #{error}

            form#forumulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") E-mail
                    - let email = campos && campos.email ? campos.email : ''
                    input(type="text" name="email" id="email" placeholder="Ingresa tu email" value=email)
                .campo
                    label(for="password") Password
                    - let password = campos && campos.password ? campos.password : ''
                    input(type="password" name="password" id="password" placeholder="Ingresa tu password" value=password)
                .campo.enviar
                    input.boton(type="submit" value="Crear cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a iniciar sesión
        
        script(src="/dist/bundle.js")
